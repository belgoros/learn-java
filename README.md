## A Java project containing examples of use:

   * Java patterns
   * essential file operations (reading, writing , renaming)
   * use of log
   * read properties file
   * work with JSON, XML and CSV files